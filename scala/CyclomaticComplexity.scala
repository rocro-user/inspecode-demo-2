object CyclomaticComplexity {

  def main(args: Array[String]) {
    var a, a1, a2, b, b1, b2, c, c1, d, d1, e, f, n, h, z = 0
    if (a == b)  {
      if (a1 == b1
          && c1 == d1) {
        fiddle()
      } else if (a2 == b2
          || c1 < d1) {
        fiddle()
      } else {
        fiddle()
      }
    } else if (c == d) {
      while (c == d) {
        fiddle()
      }
    } else if (e == f) {
      whie (n < h
          || n < 6) {
        fiddle()
        n++
      }
    } else {
      z match {
        case 1 => fiddle()
        case 2 => fiddle()
        case _ => fiddle()
      }
    }
  }

  def fiddle() {}
}
